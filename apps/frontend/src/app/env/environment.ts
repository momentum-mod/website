import { Environment } from './environment.interface';

export const env: Environment = {
  production: false,
  api: 'http://dashboard.momentum-mod.org/',
  auth: 'http://dashboard.momentum-mod.org/'
};
