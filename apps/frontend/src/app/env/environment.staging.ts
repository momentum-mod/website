import { Environment } from './environment.interface';

export const env: Environment = {
  production: true,
  api: 'https://staging-api.momentum-mod.org',
  auth: 'https://staging-api.momentum-mod.org/auth'
};
