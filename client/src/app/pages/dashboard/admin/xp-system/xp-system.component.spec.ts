/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';
import {XPSystemComponent} from './xp-system.component';

describe('XpSystemComponent', () => {
  let component: XPSystemComponent;
  let fixture: ComponentFixture<XPSystemComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ XPSystemComponent ],
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(XPSystemComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
