version: '3.8'

services:
  db:
    image: mysql:8
    volumes:
      - ./mysqlxd:/var/lib/mysql

  api:
    depends_on:
      - db
    restart: always
    env_file:
      - .env
    ports:
      - ${NODE_PORT}:${NODE_PORT}
